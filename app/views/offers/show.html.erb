<div class="map" style="background-image: url('<%= if @offer.image.attached?
      cl_image_path(@offer.image.key)
    elsif @offer.url_image.nil?
      'https://images.pexels.com/photos/691668/pexels-photo-691668.jpeg?auto=compress&cs=tinysrgb&w=1260&h=750&dpr=1'
    else
      @offer.url_image.strip
    end %>');">
</div>
<div class="container margin-top-negative">
  <div class="row">
    <div class="col-8">
      <div class="offer-show-card">
        <h1><%= @offer.name %></h1>
        <hr>
        <div class="d-flex">
        <%= render 'layouts/set_vava', vava: @offer.user %>
        <p class="fw-bold mx-3 pt-2">Hôte: <%= @offer.user.username  %> </p>
        </div>
        <p><%= @offer.description %></p>
<<<<<<< HEAD
        <p class="price"><i class="fa-solid fa-coins" style="color: #000000;"></i><%= @offer.price.to_s.reverse.gsub(/(\d{3})(?=\d)/, '\\1,').reverse %> *</p>
        <p class="little_text"><i>* le prix n'est pas imaginaire</i></p>
        <br>
        <p class="offer-show-address"><%= @offer.address %></p>
=======
        <div class="d-flex justify-content-between">
        <p class="offer-show-address mb-0"><i class="fa-solid fa-location-dot fa-bounce icon-blue px-1"></i><%= @offer.address %></p>
        <p class="price mb-0"><i class="fa-solid fa-coins icon-yellow fa-beat px-1"></i><%= @offer.price %></p>
        </div>
>>>>>>> d2ad0fe110fff5499dabeb097cbbb0dbb913dee3
      </div>
    </div>
    <div class="col-4">
      <div class="offer-show-card">
        <h5>Reservation</h5>
          <%= simple_form_for [@offer, @booking] do |f| %>
            <%= f.input :start_date, input_html: {data: {controller:'datepicker', datepicker_target: "start"}, placeholder: "select dates"}, as: :string %>
            <%= f.input :end_date, input_html: {data: {controller:'datepicker', datepicker_target: "end"}, placeholder: "select dates"}, as: :string %>
            <%= f.submit "Book", class: "btn btn-primary rounded" %>
          <% end %>
      </div>
    </div>
  </div>
<%# commentaires %>
  <div class="overflow-auto mb-3" style="height: 200px;">
    <% @reviews.each do | review | %>
      <div class="card mb-4 shadow-sm">
        <div class="card-body">
          <div class="d-flex align-items-center mb-2">
            <!-- Avatar et Nom d'utilisateur -->
            <div class="d-flex align-items-center me-3">
              <div class="me-2">
                <%= render 'layouts/set_vava', vava: review.user %> <!-- Avatar du user -->
              </div>
              <h5 class="mb-0">
                <%= review.user.username %> <!-- Nom d'utilisateur -->
              </h5>
            </div>

            <!-- Évaluation (rating) -->
            <div>
              <span class="badge bg-primary">
                <%= review.rating %> ★
              </span>
            </div>
          </div>

          <!-- Contenu de la review -->
          <p class="card-text">
            <%= review.content %>
          </p>
        </div>
      </div>
    <% end %>
  </div>

  <div class="mt-5 " style="width: 100%; height: 600px;"
    data-controller="map"
    data-map-markers-value="<%= @markers.to_json %>"
    data-map-api-key-value="<%= ENV['MAPBOX_API_KEY'] %>">
  </div>
</div>
